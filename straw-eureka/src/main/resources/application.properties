#配置eureka的端口号,它也是一个tomcat项目,也能够输入路径访问
#只不过里面的代码是写好的,不用管
#8761是微服务体系中注册中心默认的端口号
server.port=8761

# 作为eureka服务器不需要进行对自己的注册
eureka.client.register-with-eureka=false

# 因为注册中心只提供其他服务的发现和调用，而注册中心本身不参与其他服务的工作；
# 所以注册中心不需要抓取注册列表
eureka.client.fetch-registry=false

#其他微服务项目都不需要写上面的两个，因为默认为true
